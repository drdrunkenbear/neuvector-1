defaultNamespace: neuvector

helm:
  chart: core
  repo: https://neuvector.github.io/neuvector-helm


  version:  2.2.0 
  releaseName: neuvector
  values:
    registry: docker.io 
    tag: 5.0.0

    controller:
      replicas: 2

    manager:
      svc:
        type: NodePort

    cve:
      scanner:
        replicas: 2

    k3s:
      enabled: false
    
