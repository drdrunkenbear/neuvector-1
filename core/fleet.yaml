defaultNamespace: neuvector

helm:
  chart: core
  repo: https://github.com/Anjum4567/neuvector-helm.git
  version: 2.4.0 
  releaseName: neuvector
  values:
    registry: docker.io 
    tag: 5.1.0

    controller:
      replicas: 3

    manager:
      svc:
        type: NodePort

    cve:
      scanner:
        replicas: 3

    k3s:
      enabled: false
    
